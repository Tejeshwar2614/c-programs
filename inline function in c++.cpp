#include <iostream>
using namespace std;

inline int product(int a,int b)
{
	int c = 0;
	c=c+1;
	return a*b+c;
}
int main()
{
	int a=10,b=20;
	cout<<"the product of the two number is : "<<product(a,b)<<endl;
    cout<<"the product of the two number is : "<<product(a,b)<<endl;
	cout<<"the product of the two number is : "<<product(a,b)<<endl;
	cout<<"the product of the two number is : "<<product(a,b)<<endl;
	cout<<"the product of the two number is : "<<product(a,b)<<endl;
}
